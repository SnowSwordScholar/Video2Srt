[project]
name = "video2srt"
version = "0.1.0"
description = "Local batch video transcription and SRT generation."
requires-python = ">=3.10,<3.14"
dependencies = [
    "faster-whisper==1.2.1",
    "huggingface-hub>=0.24,<1.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6,<7",
]

[tool.uv]
package = false
